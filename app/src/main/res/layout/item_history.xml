<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="8dp">

    <!-- Checkbox seleksi di luar card -->
    <com.google.android.material.checkbox.MaterialCheckBox
        android:id="@+id/cbSelect"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginEnd="8dp"
        android:visibility="gone"
        android:focusable="false"
        android:clickable="false" />

    <!-- Card utama -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        app:cardCornerRadius="16dp"
        app:cardUseCompatPadding="true"
        app:cardElevation="3dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Bar atas: icon app + nama + waktu + pill skor -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageView
                    android:id="@+id/ivAppIcon"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:layout_marginEnd="12dp"
                    android:src="@mipmap/ic_launcher"
                    android:contentDescription="@string/app_name" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/tvAppName"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="WhatsApp"
                        android:textStyle="bold"
                        android:textSize="14sp"
                        android:textColor="@color/ls_text_primary" />

                    <TextView
                        android:id="@+id/tvTime"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="24/12/25 11:54"
                        android:textSize="12sp"
                        android:textColor="@color/ls_text_secondary" />
                </LinearLayout>

                <!-- Pill skor -->
                <TextView
                    android:id="@+id/tvScorePill"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="@drawable/bg_score_pill"
                    android:paddingLeft="10dp"
                    android:paddingRight="10dp"
                    android:paddingTop="4dp"
                    android:paddingBottom="4dp"
                    android:text="65% (HIGH)"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:textColor="#C62828" />
            </LinearLayout>

            <!-- URL yang discan -->
            <TextView
                android:id="@+id/tvOriginalUrl"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="https://bit.ly/xxxx"
                android:textSize="13sp"
                android:textColor="@color/ls_text_primary"
                android:maxLines="2"
                android:ellipsize="end" />

            <!-- URL final (kalau beda) -->
            <TextView
                android:id="@+id/tvResolvedUrl"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="→ docs.google.com/forms/xxxxx"
                android:textSize="12sp"
                android:textColor="@color/ls_text_secondary"
                android:maxLines="2"
                android:ellipsize="end"
                android:visibility="gone" />

            <!-- Host + algoritma -->
            <TextView
                android:id="@+id/tvHostAlgorithm"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="docs.google.com • XGBOOST"
                android:textSize="12sp"
                android:textColor="@color/ls_text_secondary" />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

</LinearLayout>